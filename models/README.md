Place local model files here if using GGUF/manual Ollama Modelfile mode.

Recommended first path:
- Use Ollama model names instead of manual files:
  - qwen3:8b
  - deepseek-r1:8b

If using GGUF:
- Put the file here, e.g. models/fight-ref.gguf
- Create models/Modelfile later.
